import { LogSearchPipe } from './log-search.pipe';

describe('LogSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new LogSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
